
fn pow (base, exp) {
    result = {"res": base};
    for (i = 0; i<exp; i = i + 1;) {
        r  = result["res"] * base;
        result["res"] = r;
        println("result:",r);
    }
    return result;
}

println(pow(2, 4));



sum = 0;
for (i = 0; i < 10; i = i + 1) {
    sum = sum + i;
};

println(sum);
